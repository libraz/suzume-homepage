{
  "name": "suzume-homepage",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev",
    "build": "node scripts/generate-og-image.mjs && vitepress build",
    "build:og": "node scripts/generate-og-image.mjs",
    "preview": "vitepress preview",
    "copy:wasm": "./scripts/copy-wasm.sh",
    "update": "yarn copy:wasm && yarn dev",
    "test": "yarn copy:wasm && vitest run"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "opentype.js": "^2.0.0",
    "vitepress": "^1.6.4",
    "vitest": "^4.1.10",
    "vue": "^3.5.40"
  },
  "packageManager": "yarn@4.15.0"
}
